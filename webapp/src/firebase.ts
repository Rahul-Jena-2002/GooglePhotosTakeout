/**
 * @file firebase.ts
 * Main Firebase entrypoint. Re-exports everything from modular src/lib/firebase/
 * to maintain 100% backward compatibility with all existing imports.
 */
export * from './lib/firebase';
