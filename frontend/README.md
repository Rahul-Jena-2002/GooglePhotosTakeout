// Note: In a real environment, I would run 'npm create vite@latest . -- --template react-ts'
// and 'npm install tailwindcss postcss autoprefixer'
// Since I am an AI agent in a CLI, I will provide the core React components and setup files.
// These files should be placed in the frontend/src directory.
